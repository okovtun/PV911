catch
CPU, OS, C++ function.

try
{
    //Потенциально опасный код;
    //Или код, который может бросить (throw) исключение (exception)
}
catch(type1 exception)
{
    //код, обрабатывающий исключение;
}
catch(type2 exception)
{
    //код, обрабатывающий другое исключение;
}
....
....
catch(typeN exception)
{
   //еще какой-то код, обрабатывающий еще какое-то исклюбчение;
}
catch(...)
{
    //Обрабатывает все, что не было обработано выше;
    //как deafult в switch-е)).
    //deprecated
}
